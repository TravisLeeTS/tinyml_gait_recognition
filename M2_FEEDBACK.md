# Milestone #2 — Detailed Feedback

## Group 3 — Energy-Efficient Human Activity Recognition on the Edge (Track B, UCI-HAR primary + WISDM secondary)

**Total: 92/100 — Excellent**

## Rubric
| # | Component | Score | Justification |
|---|---|---|---|
| 1 | Dataset description & Data Card (R1, R2) | 18/20 | Section 1.1 defines all 12 classes (6 UCI-HAR + 6 WISDM) with precise physical descriptions. Table 2 gives per-class train/val/test counts for UCI-HAR (total 10,299). Section 1.3 covers Track B dataset identity, licenses (CC BY 4.0 for UCI-HAR, citation-only for WISDM), dataset characteristics, domain gap, and the Arduino collection plan. Data Card (Table 3) fills all required fields. **Two deductions: (a) the split-ratio field is internally contradictory — Table 3 states "UCI HAR: 70% / 30% (train/test; no validation set)", but Table 2 shows an explicit three-way split (5551/1801/2947) and Section 3 item 5 further states "a validation set is created from the training subset using a 75%/25% split with subject-aware grouping"; three different split descriptions in the same 8-page report. (b) Per-class sample counts are given for UCI-HAR but not for WISDM, even though the Data Card lists WISDM (5,424 windowed samples) and it is discussed throughout Section 1.3 — the handout's "Common mistake" box explicitly requires a per-class breakdown for any dataset reported. (The WISDM breakdown exists as `docs/tables/wisdm_classic_raw_counts.csv` in the repo but is not surfaced in the report.)** |
| 2 | Preprocessing pipeline (R3) | 10/10 | Section 3 specifies 50 Hz, 128-sample windows (2.56 s) with 50% overlap, uses official pre-windowed inertial signals, selects 6 channels (total_acc + body_gyro x/y/z), uses raw sequences to reduce on-device cost. Normalization (standardize-then-min-max) computed on training set only and applied unchanged to val/test — explicitly leakage-free. |
| 3 | Baseline model description (R4) | 8/10 | Section 4 and Table 5 describe two model paths: a paper-reproduction baseline (ConvLSTM + CNN-GRU + CNN-BiGRU + CNN-BiLSTM + CNN-LSTM stacking with XGBoost meta-learner, cited to [1]) and a lightweight depthwise-separable 1D CNN (1,922 Keras parameters). Training hyperparameters are complete: Adam with lr=0.001 and decay 0.01, batch size 50, sparse categorical cross-entropy loss, 20 epochs (bounded) for the stacking baseline, 40 epochs with patience 8 for the lightweight model. Input/output shapes ([128, 6] → 6-class softmax) and rationale (strong offline reference vs. TFLite-deployable candidate) are stated. **Deduction: layer-level architecture is missing from the report for both paths — no filter counts, kernel sizes, strides, depthwise-separable block composition, dropout rates, or intermediate activation functions (only the output softmax is implicit). The five stacking base learners are cited by name only with no layer structure for any of them.** A reader cannot reconstruct the architectures from the report alone; the rubric explicitly requires "layers, sizes, activation functions" (they exist in `src/models/...` but not in-report). |
| 4 | Test-set results with metrics (R5) | 25/25 | Section 5 reports test-set results on 2,947 windows from 9 officially held-out UCI-HAR subjects (matches the Data Card exactly). Table 7 gives per-class precision, recall, F1 with support and both macro and weighted averages for *both* models (Baseline Acc 0.914, Macro F1 0.9128, Weighted F1 0.9137; 1D CNN Acc 0.917, Macro F1 0.9173, Weighted F1 0.9168). Figures 1 and 2 provide confusion matrices with True (rows) / Predicted (columns). All five R5 rubric criteria are fully satisfied, and the dual-path presentation (paper-reproduction upper-bound vs. deployable lightweight candidate) is clearly labeled in Section 4 rather than confusing. |
| 5 | Error analysis (R6) | 14/15 | Section 6 correctly identifies SITTING↔STANDING as dominant and WALKING_UPSTAIRS↔WALKING_DOWNSTAIRS as secondary, with plausible causes (static-posture similarity, gravity-orientation reliance, gait overlap). Next steps are concrete (feature selection for static discrimination, focal loss). Domain-gap section flags hardware differences. Deduction: analysis depth is limited to posture confusion; walking-related confusions (17 Walking→Downstairs in baseline, 43 in README) are mentioned but not deeply diagnosed. |
| 5b | Arduino plan (R7) | 4/5 | Plan: LSM9DS1 accelerometer + gyroscope at 50 Hz, waist-mounted, 2.56 s windows with 50% overlap, week of 22–23 April, all six UCI activity classes enumerated. A fallback **is** present in Section 7.1 (calibration, mixed public + Arduino retraining, accelerometer-only ablation). **Deduction: the plan is internally inconsistent — Section 1.3 states "1–2 users, ≥100 labeled windows per class" while Table 8 states "3–5 users, ≥50 labelled windows per class".** Same plan, two different target numbers — must be reconciled before M3 execution. |
| 6 | Code and data quality (D2, D3, D4) | 9/10 | `requirements.txt` pins every library with `==` (numpy 2.0.0, TF 2.18.0, Keras 3.8.0, xgboost 2.1.3). `README_MILESTONE2.md` is extensive with CLI commands, reproduction steps, outputs mapping, and explicit D2/D3/D4 deliverable mapping. `src/` is well-organized as a proper Python package (data, models/{reproduction,lightweight}, training, utils), and the repo already contains early M3 deployment scaffolding (`platformio.ini`, `wokwi.toml`, `src/main.cpp`, populated `.pio/build/`). Deductions: the full dataset archives `data/raw/UCI_HAR_Dataset.zip` and `data/raw/WISDM_ar_latest.tar.gz` are committed despite the README claiming they are Git-ignored — they should be removed and fetched via the download step; and no trained-model artifacts (weights, TFLite, metrics JSONs, confusion-matrix PNGs) are committed, so graders must re-run training to inspect numbers. |
| 7 | Report clarity and formatting | 4/5 | Well-organized per R1–R7 plus an Ethics & Limitations section, all 8 tables and 2 figures captioned, no placeholder text, concise writing. The standalone PDF is self-contained — scientific content (Data Card, preprocessing, baselines, results, error analysis, M3 plan) is fully covered in the report; the README is a reproduction guide, not a stand-in for report content. Deductions are polish-level: in Table 3 the Data Card URL `https://archive.ics.uci.edu/dataset/240/human+activity+recognition+using+smartphones` overflows the cell and wraps mid-word, and the typo "22-23th April" appears twice (Section 1.3 text and Table 8 context). |

## Strengths
- Strong, principled preprocessing: explicit train-only normalization (`SequenceStandardizer.fit(data.train.x)` confirmed in `src/utils/normalization.py`), subject-aware validation split from official training subjects, leakage-free handling of the official UCI-HAR test split.
- Clear two-track design (paper-reproduction upper-bound + deployable 1,922-parameter depthwise-separable 1D CNN) with an explicit **four-phase development roadmap (Table 6)** mapping M2→M3→M4 to accuracy, latency, energy, and deployment evaluation foci.
- **M3 hardware scaffolding already in place**: committed `platformio.ini`, `wokwi.toml`, `src/main.cpp`, and a populated `.pio/build/` — the team is genuinely ahead on deployment prep compared to most peers at M2.
- Excellent reproducibility posture: `==`-pinned requirements, CLI entry points (`python -m src.training.train_reproduction` / `train_lightweight`), deterministic output-directory mapping, dedicated Milestone-2 README documenting D2/D3/D4.
- Honest treatment of WISDM: explicitly *not* forcing it into the UCI label set, explicitly *not* training on it at M2 after the timestamp audit showed irregular effective sampling.

## Key improvements for M3 (aligned to the M3 rubric)
- **Working demo + evidence (D2, D3 — 25 pts):** Flash the Arduino sketch using the lightweight depthwise-separable 1D CNN (1,922 params), not the stacking baseline; LSM9DS1 at 50 Hz, 128-sample (2.56 s) windows, 50% overlap; emit the 6 UCI classes on Serial + LED; 60–90 s video including at least one transition class. Stable 2-min run.
- **Deployment metrics (R2 — 15 pts):** Report `.tflite` size (not Keras), tensor arena, and avg `Invoke()` latency over ≥50 calls with `micros()`. Pipeline diagram: LSM9DS1 → 128-sample buffer → standardize+min-max with saved train stats → Invoke → class/LED.
- **Optimization vs M2 (R3 — 20 pts):** INT8 PTQ on the 1,922-param CNN as the primary optimization; before/after table of size/latency/RAM/macro-F1 vs the Keras FP32 baseline. As a second improvement — motivated directly by the M2 SITTING↔STANDING confusion — try focal loss or add a posture-discriminating feature (gravity-direction estimate from accelerometer mean).
- **On-device accuracy (R4 — 10 pts):** Execute the Apr 22–23 collection per M2 plan (≥100 windows/class, 1–2 users, 2 environments); ≥20 live trials/class on Arduino; live confusion matrix; report offline UCI-test vs. Arduino-live accuracy and explain the gap.
- **Robustness test (R5 — 15 pts):** Use the second environment or a user not in training data; ≥10 trials/class; analyze which classes degrade (walking-up vs walking-down expected).
- **Challenges & M4 plan (R6, R7):** Reconcile the Data Card "70/30" entry with the README's 5551/1801/2947 three-way split before submission; add per-layer architecture details for both the stacking base learners and the depthwise-separable CNN.
- **Repo & README (D4 — 10 pts):** Commit the exported `.tflite` and final metrics JSON so numbers are inspectable without re-running; add `.ino`, conversion script, flash/run README, and the explicit fallback (narrowed-class subset) in the report itself, not just `README_MILESTONE2.md`.
