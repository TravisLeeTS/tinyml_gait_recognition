.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch
__pycache__/
*.py[cod]

# Reproducible public dataset downloads.
data/raw/UCI_HAR_Dataset.zip
data/raw/WISDM_ar_latest.tar.gz
data/raw/UCI HAR Dataset/
data/raw/WISDM_ar_v1.1/
data/raw/__MACOSX/
data/raw/uci_har_download_source.txt

# Generated processed arrays.
data/processed/*.npz

# Local notebooks/runtime noise.
.ipynb_checkpoints/
.venv/

# Local sharing/submission packages.
*.zip
!outputs/m4_live_handoff_candidate/m4_group3_live_handoff.zip
# Live Arduino evidence is professor-facing submission evidence. Keep these
# files commit-eligible by default, including Session05 and future live logs.
!outputs/live_evidence/**
outputs/share/

# Oversized returned teammate evidence. Small proof videos/docs in this folder
# are intentionally committed for M3 grading; keep the large stability video in
# the course submission attachment instead of GitHub.
outputs/live_evidence/raw_teammate_return/2_30_stability_video.mp4

# Local raw Arduino captures are used to regenerate metrics, but should not be
# uploaded accidentally as repository source.
data/raw/arduino_collectdata_v*/
data/raw/arduino_live_*/
